server:
  host: "localhost"
  port: 8080

database:
  path: "data/cloudmind.db"

logging:
  level: "INFO"
  file: "logs/cloudmind.log"

llm:
  api_key: "insert your OpenAI API key in .env"
  model: "gpt-4o"
  max_tokens: 1000
  temperature: 0.7
  system_prompt: "You are a helpful assistant."
